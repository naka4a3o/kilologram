<div class="contents row">
  <div class="container">
    <h3>New Calendar</h3>

<%# <%= render 'form', calendar: @calendar %> 

<%# <%= link_to 'Back', calendars_path %>
<%# <%= form_with model: @calendar, local: true do |form| %> 
    <%= form_with (model: [@user, @calendar],local: true) do |form| %>
    <%# <%= render 'shared/error_messages', model: [@user, @calendar] f.object %>  


    <div class="title">
      <%= form.label :体重 %>
      <%= form.text_field :title %>
    </div>

    <div class="time">
      <%= form.label :日時 %>
      <%= form.datetime_field :start_time %>
    </div>

    <div class="content">
      <%= form.label :コメント %>
      <%= form.text_field :content %>
    </div>


    <div class="submit">
      <%= form.submit %>
    </div>
   <% end %>
  </div>
</div>